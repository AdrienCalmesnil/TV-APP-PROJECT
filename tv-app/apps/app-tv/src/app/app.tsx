import styled from "styled-components";
import SearchPage from "./search-page";

const StyledApp = styled.div`
  //
`;

export function App() {
  return (
    <StyledApp>
      <SearchPage />
    </StyledApp>
  );
}

export default App;
